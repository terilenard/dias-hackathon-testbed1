// Base data structures used for communication patterns

syntax = "proto2";
package dias;

message RequestHeader {
  required uint64 id = 1;
  required string name = 2;
}

message ReplyHeader {
  required uint64 id = 1;
}

